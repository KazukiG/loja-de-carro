
@{
    ViewBag.Title = "Carros";
}

<section id="" class="section-padding overlay2">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="section-title  text-center">
                    <h2>Carros</h2>
                    <span class="title-line"><i class="fa fa-car"></i></span>
                    <p>Realize a gestão de cadastro dos carros da Trilha Car</p>
                </div>
            </div>
        </div>
    </div>
</section>

<div class="container">
    <form>
        <div class="form-row">
            <div class="col-lg-12 text-right">
                <button type="button" class="btn btn-success btn-sm" data-toggle="modal" data-target=".bd-example-modal-lg">Incluir Novo Veículo</button>
            </div>
        </div>
        <br />
        <br />
        <div class="form-row">
            <div class="col-md-12">
                <table class="table table-bordered table-hover">
                    <thead>
                        <tr>
                            <th scope="col">Id</th>
                            <th scope="col">Descrição do Véículo</th>
                            <th scope="col">Versão</th>
                            <th scope="col">Ano de Fabricação</th>
                            <th scope="col">Valor Cotação/h</th>
                            <th scope="col" class="text-center">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        @*@foreach (var item in Model)
                            {*@
                        <tr>
                            <th scope="row">{{sfsdfg}}</th>
                            <td>{{sfsdfg}}</td>
                            <td>{{sfsdfg}}</td>
                            <td>{{sfsdfg}}</td>
                            <td>{{sfsdfg}}</td>
                            <td class="text-center">
                                <button type="button" class="btn btn-primary btn-sm details" @*data-id="@item.idCar"*@>Alterar</button>
                                    <button type="button" class="btn btn-warning btn-sm edit" @*data-id="@item.idCar"*@>Detalhar</button>
                                    <button type="button" class="btn btn-danger btn-sm delete" @*data-id="@item.idCar"*@>Excluir</button>
                            </td>
                        </tr>
                        @*}*@
                    </tbody>
                </table>
            </div>
        </div>
    </form>
</div>

<div class="modal fade bd-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            @Html.Partial("_partialCreateCars")
        </div>
    </div>
</div>
